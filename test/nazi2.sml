class Test {
  def main() : int {
    return 0;
  }
}

class Book {
  var j : int[];
  var k : boolean;

  def x (f1: int) : int {
    if (3||true) then
      k = false;
    else
      k = "YES";
    return -A && !V || (3+4-3*5);
  }

  def f(): string {
    while ( i <> 9){
      writeln("hi");
    }
    return (B||C) + (m*9) - 3;
  }
}