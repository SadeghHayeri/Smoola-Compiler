class Test {
  def main() : int {
    return 0;
  }
}

class Book {
  var j : int[];
  var k : boolean;
  def x (f1: int) : int {
    j = 5;
    while (i.length < 4){
      j = j && (3*2);
    }
    return 3+5+(3||true);
  }
}